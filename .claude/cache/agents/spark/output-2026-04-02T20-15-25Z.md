# Quick Fix: Web App — Favicon, Starfield, Pricing Button, OudenOS Naming
Generated: 2026-04-02T20-15-25Z

## Changes Made

### 1. New Favicon SVG — Ouden geometric mark
- File: `apps/web/public/icon.svg`
- Change: Replaced old hexagon/orbital design with the new Ouden mark: black rounded-rect bg, grey square outline, red circle outline, red dot center (square → circle → dot hierarchy)

### 2. Star field utility class
- File: `apps/web/src/app/globals.css`
- Lines: after `.nd-dot-grid-subtle` block
- Change: Added `.nd-starfield` class with 16 radial-gradient dots at random positions (0.5px–1.5px, 7–20% opacity)

### 3. Starfield on html element (global background)
- File: `apps/web/src/app/globals.css`
- Change: Extended `html` rule to include 14-dot starfield via `background-image` (4–10% opacity, dimmer than the utility class) with `background-attachment: fixed`

### 4. Download button visibility fix
- File: `apps/web/src/components/sections/PricingSection.tsx`
- Line: ~84
- Change: OudenOS card download button — removed `border border-[var(--border)] bg-[var(--surface-raised)] text-[var(--text-primary)]`, replaced with `bg-[#D71921] text-white hover:bg-[#B5141B]`. Button text changed from "Download Free" to "Download OudenOS"

### 5. OudenOS naming audit
- Searched all `.tsx` and other source files in `apps/web/src/` for "Ouden OS" (with space)
- Result: No instances found — naming was already correct everywhere (✓ VERIFIED)
- Logo.tsx correctly shows "ouden.cc" for the website wordmark
- All product references throughout sections/pages already use "OudenOS" (one word)

## Verification
- No TypeScript compilation needed for CSS/SVG changes
- PricingSection.tsx edit is a class string swap — no logic change

## Files Modified
1. `apps/web/public/icon.svg` — new Ouden geometric mark SVG
2. `apps/web/src/app/globals.css` — starfield utility class + html background starfield
3. `apps/web/src/components/sections/PricingSection.tsx` — download button accent red + text fix
