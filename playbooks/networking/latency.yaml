# Networking: Latency Optimization
# ALL latency tweaks REMOVED — they break connectivity on too many systems.
# Nagle, offloading, RSS queues — none of these are safe to apply blindly.

module: networking.latency
name: "Network Latency Optimization"
description: "Network latency tweaks — currently disabled for safety"
category: networking

actions: []
